[project]
name = "dataset-cleaning"
version = "0.1.0"
description = "Read-only EDA to validate transliteration/feature cleaning rules before touching postprocessing."
requires-python = ">=3.12"
dependencies = [
    "orjson>=3.10.0",
    "rich>=14.1.0",
    "unidecode>=1.3.8",
    "numpy>=2.0.0",
]
